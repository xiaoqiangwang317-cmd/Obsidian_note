---
up:
  - 数据库
related:
date: 2026-05-08
---

Redis 是一种[[内存数据库|基于内存的数据库]]，对数据的读写操作都是在内存中完成，因此读写速度非常快，**常用于缓存，消息队列、**[[分布式锁]]等场景。

常见是数据类型包括 String，[[Redis-List(列表)|List(列表)]]，[[Redis-Hash|Hash]]，Set（集合），Zset（有序集合）
[[Redis的架构]]包含四种，这四种有不同的适用场景
在进行持久化时Redis这种单线程应用使用[[fork()|Forking]]进行高效的持久化。


