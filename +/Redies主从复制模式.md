---
up:
  - "[[Redis的架构]]"
related:
  - 主从架构
date: 2026-05-08
---
## 架构样式
![[image-1778229966472.webp|400x258]]

## 工作原理
主节点（Master）负责：
- 写操作
- 数据更新
从节点（Slave）负责：
- 复制主节点数据
- 读操作（缓解了主节点的读压力）
## 数据同步方式
1. 全量同步-->基于[[RDB快照]]
2. 增量同步-->基于[[AOF]]
> [! Note]
> 1. Redis的全量复制发生在Slave（从节点）初始化阶段，此时slave需要将master主机上的所有数据都复制一份。
> 2. 增量复制：主服务器每执行一个写命令就会向从服务器发送一个相同的写命令，从服务器接收并执行收到的写命令。
> 3. 主从刚刚连接的时候，进行全量同步；全同步结束后，进行增量同步。当然，如果有需要，slave 在任何时候都可以发起全量同步。
> 4. redis 策略是，无论如何，**首先会尝试进行增量同步**，如不成功，要求从机进行全量同步。

## 机制流程
![[image-1778230742970.webp|604|604]]
主节点会有一个复制Id（用于从节点验证复制从哪里来）和偏移量（用于增量同步）。
> [! Note] 
> 每当Master发生写变化时，将变化追加到replication stream（复制流）中